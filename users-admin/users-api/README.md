# users-api

## scaffolding

```shell
mkdir users-api
cd users-api
npm init --yes
```

### after having appropriately modified the x file, add the dependencies to the project with the command:

```shell
npm ci
```
